{
  "name": "@website-factory/orchestrator",
  "private": true,
  "version": "0.0.0",
  "description": "Orchestrator worker: Cloudflare Workflow definitions, queue consumers and cron (implementation starts in M3/M5)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist --env=''",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "wrangler": "^4.115.0"
  },
  "dependencies": {
    "@website-factory/core": "workspace:*",
    "@website-factory/db": "workspace:*"
  }
}
