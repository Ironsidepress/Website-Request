/**
 * @website-factory/testing — Shared test factories, fixtures, in-memory WorkflowRunner and clock (implementation starts in M1).
 *
 * M0 scaffolding only: no implementation lives here yet by explicit instruction.
 */
export const PACKAGE_NAME = '@website-factory/testing' as const;
