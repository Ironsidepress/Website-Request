/**
 * @website-factory/agents — Agent contracts, versioned prompts and the SimulatedExecutor (implementation starts in M5).
 *
 * M0 scaffolding only: no implementation lives here yet by explicit instruction.
 */
export const PACKAGE_NAME = '@website-factory/agents' as const;
