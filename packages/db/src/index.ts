/**
 * @website-factory/db — Drizzle schema, migrations and tenant-scoped repositories (implementation starts in M1).
 *
 * M0 scaffolding only: no implementation lives here yet by explicit instruction.
 */
export const PACKAGE_NAME = '@website-factory/db' as const;
